
<% if user_signed_in? %>
		<li><%= link_to current_user.email, home_dashboard_path %></li>
    	<li><%= link_to('Logout', destroy_user_session_path, :method => :delete) %></li>
<% else %>
    <%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => {:class => "new_user navbar-form pull-right"} ) do |f| %>
	  <%= f.email_field :email, {:class => "span2", :id => "email", :placeholder => "Email"} %>	
	  <%= f.password_field :password, {:class => "span2", :id => "password", :placeholder => "Password"} %>
	  <%= f.submit "Sign in", {:class => "button expand", :id => "signin"} %>
	  <%= link_to('Register', new_user_registration_path, {:class => "success button expand", :id => "register"}) %>
	<% end %>
<% end %>
