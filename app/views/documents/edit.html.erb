<h1>Editing document</h1>

<%= render 'form' %>

<p>
	<b>Owner:</b>
	<% if @document.user %>
		<%= @document.user.email %> 
	<% end %>
</p>

<%= link_to 'Show', @document %> |
<%= link_to 'Back', documents_path %>
