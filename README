Visit http://seniordesign.engr.uidaho.edu/2011-2012/EcoData for information.

This project requires Ruby >= 1.9.2 and Rails 3.1.3.

Requirements:
CouchDB
Ruby >= 1.9.2
Rails >= 3.1.2
bundler gem

To install and run:
    bundle install
    rake db:setup
    rails server

Then visit at http://localhost:3000/ (by default).
